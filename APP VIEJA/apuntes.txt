modelo Vista controlador

vista: muestra al usuario, la logica(en el medio) y el controlador: recibe los input y 
los procesa(puede devolver o no algo)

lenguaje solo PHP

$pdo ES:
/*php data objet*/
ES UN TIP DE OBJETO, CAPAZ DE MANIPULAR DATOS Y CONECTARSE A UNA DB

para la manipulacion de datos se van a usar los modelos, que operan la logica de una aplicacion
___________________________________________________________________________________________________

User es una clase, como queremos extraer de la bd usamos $pdo

EXAMPLE:
<?php
//User es una clase, es la representacion de un usuario

class User {
    private $pdo;

    public function __construct($pdo){
        $this->pdo/*este es de el private $pdo*/ = $pdo; //el propio obj. de this user
    }

    public function getPdo(){
        return $this-> pdo;
    }
}

$prueba2 = new User("hola");
?>

 $stmt es resultado en en.




esto es como un if    
$action = true ? "hola" : "chau";    

controlador: maneja el trafico, etc.

recibe los imputs y en base a eso hace X acciones que son procesadas por el MODELO



hacer el modificar y eliminar ALUMNOS

MODELo da que acciones que hay
controlador indica en cuando se lanza cada operacion


variables de sesion: almacenan en el navegador los datos 
es un solo array que guarda muchos datos 

siempre tratar de minimizar las operaciones en el servidor